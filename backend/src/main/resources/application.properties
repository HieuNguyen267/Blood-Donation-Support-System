# Server Configuration
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/blood_donation_system?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=nhattu@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.h2.console.enabled=true

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Data initialization
spring.jpa.defer-datasource-initialization=false
spring.sql.init.mode=never

# JWT Configuration
app.jwtSecret=BloodDonationSecretKey2024VeryLongAndSecureForHS512AlgorithmWithAtLeast512Bits
app.jwtExpirationInMs=86400000

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging Configuration
logging.level.org.springframework=INFO
logging.level.com.blooddonation.backend=DEBUG
logging.level.org.springframework.security=DEBUG

# CORS Configuration
app.cors.allowed-origins=http://localhost:3000

# Email Configuration (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=swp391.donateblood@gmail.com
spring.mail.password=ahei ykjt dqim ygwb
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Email verification settings
app.email.verification.enabled=true
app.email.verification.required=false

# SMS Configuration
# Tắt chế độ simulation để gửi SMS thật
sms.simulation.enabled=false

# SMS Provider (twilio, viettel, vnpt, fpt)
sms.provider=twilio

# Twilio Configuration (REAL SMS)
# Để gửi SMS thật, thay thế các giá trị sau:
twilio.account_sid=REDACTED
#twilio.auth_token=REDACTED
twilio.phone_number=REDACTED

# Viettel SMS Configuration (Alternative)
sms.viettel.api_url=https://api.viettel.com/sms/send
sms.viettel.username=your_viettel_username
sms.viettel.password=your_viettel_password

# VNPT SMS Configuration (Alternative)
sms.vnpt.api_url=https://api.vnpt.com/sms/send
sms.vnpt.username=your_vnpt_username
sms.vnpt.password=your_vnpt_password

# FPT SMS Configuration (Alternative)
sms.fpt.api_url=https://api.fpt.com/sms/send
sms.fpt.username=your_fpt_username
sms.fpt.password=your_fpt_password

# Swagger Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method